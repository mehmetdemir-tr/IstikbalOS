name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTUP_TOOLCHAIN: nightly

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm grub-common xorriso qemu-system-x86 binutils llvm mtools
          echo "=== Debug: mformat location ==="
          which mformat || ls -la /usr/bin/mformat || echo "mformat not found"
          ls /usr/share/mtools/ 2>/dev/null || echo "No mtools share dir"
          export PATH="$PATH:/usr/bin:/bin"
          echo "PATH=$PATH"

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          source "$HOME/.cargo/env"
          rustup component add rust-src --toolchain nightly
          rustup component add llvm-tools-preview --toolchain nightly
          echo "Rust version: $(rustc --version)"
          echo "Cargo version: $(cargo --version)"

      - name: Build IstikbalOS
        run: |
          source "$HOME/.cargo/env"
          chmod +x build.sh
          ./build.sh

      - name: Verify ISO
        run: |
          ls -la build/
          test -f build/istikbalos.iso && ls -lh build/istikbalos.iso

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: istikbalos-iso
          path: build/istikbalos.iso
          retention-days: 30

  test-bios:
    name: Test BIOS
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download ISO
        uses: actions/download-artifact@v4
        with:
          name: istikbalos-iso
          path: build

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y qemu-system-x86

      - name: Create disk images
        run: |
          mkdir -p build
          dd if=/dev/zero of=build/istikbalos_disk.img bs=1M count=128 status=none
          dd if=/dev/zero of=build/istikbalos_disk2.img bs=1M count=256 status=none

      - name: Boot test (BIOS)
        run: |
          timeout 60 qemu-system-x86_64 \
            -m 256M \
            -cdrom build/istikbalos.iso \
            -drive file=build/istikbalos_disk.img,format=raw,if=ide,index=0,media=disk \
            -drive file=build/istikbalos_disk2.img,format=raw,if=ide,index=1,media=disk \
            -boot d \
            -nographic \
            -serial file:build/serial.log \
            2>&1 | tee build/qemu_test.log || true
          cat build/qemu_test.log
          cat build/serial.log || true

      - name: Verify boot
        run: |
          BOOT_LOG=$(cat build/qemu_test.log build/serial.log 2>/dev/null)
          if echo "$BOOT_LOG" | grep -qi "BerkeOS"; then
            echo "PASS: BerkeOS detected in BIOS boot output"
            exit 0
          else
            echo "FAIL: BerkeOS not found in boot output"
            cat build/qemu_test.log
            cat build/serial.log || echo "(no serial log)"
            exit 1
          fi

  test-uefi:
    name: Test UEFI
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download ISO
        uses: actions/download-artifact@v4
        with:
          name: istikbalos-iso
          path: build

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y qemu-system-x86 ovmf

      - name: Create disk images
        run: |
          mkdir -p build
          dd if=/dev/zero of=build/istikbalos_disk.img bs=1M count=128 status=none
          dd if=/dev/zero of=build/istikbalos_disk2.img bs=1M count=256 status=none

      - name: Find OVMF
        run: |
          # Try common system OVMF paths first
          for path in /usr/share/OVMF/OVMF.fd /usr/share/qemu/ovmf-x86_64.bin /usr/share/edk2/x64/OVMF.fd /usr/share/OVMF/OVMF_X64.fd; do
            if [ -f "$path" ]; then
              echo "OVMF_PATH=$path" >> $GITHUB_ENV
              echo "Found OVMF at: $path"
              exit 0
            fi
          done
          
          # Try to find any .fd file
          OVMF_PATH=$(find /usr/share -name "*.fd" 2>/dev/null | grep -i ovmf | head -1)
          if [ -n "$OVMF_PATH" ] && [ -f "$OVMF_PATH" ]; then
            echo "OVMF_PATH=$OVMF_PATH" >> $GITHUB_ENV
            echo "Found OVMF at: $OVMF_PATH"
            exit 0
          fi
          
          echo "No OVMF found in system. Downloading from rust-osdev/ovmf-prebuilt..."
          
          # Download OVMF from rust-osdev/ovmf-prebuilt releases (most reliable source)
          OVMF_URL="https://github.com/rust-osdev/ovmf-prebuilt/releases/download/edk2-stable202602-r1/OVMF-x86_64.tar.gz"
          curl -fSL "$OVMF_URL" -o /tmp/ovmf.tar.gz
          
          if [ ! -s /tmp/ovmf.tar.gz ]; then
            echo "ERROR: Failed to download OVMF from $OVMF_URL"
            exit 1
          fi
          
          mkdir -p /tmp/ovmf
          tar -xzf /tmp/ovmf.tar.gz -C /tmp/ovmf
          
          # Use code.fd which is the firmware code (combined with vars.fd for persistent storage)
          if [ -f /tmp/ovmf/x64/code.fd ]; then
            echo "OVMF_PATH=/tmp/ovmf/x64/code.fd" >> $GITHUB_ENV
            echo "Downloaded OVMF to: /tmp/ovmf/x64/code.fd"
            exit 0
          fi
          
          # Fallback: find any .fd file
          OVMF_PATH=$(find /tmp/ovmf -name "*.fd" 2>/dev/null | head -1)
          if [ -n "$OVMF_PATH" ] && [ -f "$OVMF_PATH" ]; then
            echo "OVMF_PATH=$OVMF_PATH" >> $GITHUB_ENV
            echo "Downloaded OVMF to: $OVMF_PATH"
            exit 0
          fi
          
          echo "ERROR: Could not find OVMF firmware after download"
          exit 1

      - name: Boot test (UEFI)
        run: |
          timeout 60 qemu-system-x86_64 \
            -m 256M \
            -cdrom build/istikbalos.iso \
            -drive file=build/istikbalos_disk.img,format=raw,if=ide,index=0,media=disk \
            -drive file=build/istikbalos_disk2.img,format=raw,if=ide,index=1,media=disk \
            -boot order=c,menu=off \
            -nographic \
            -serial file:build/serial.log \
            -bios "$OVMF_PATH" \
            2>&1 | tee build/qemu_test.log || true
          cat build/qemu_test.log
          cat build/serial.log || true

      - name: Verify boot
        run: |
          BOOT_LOG=$(cat build/qemu_test.log build/serial.log 2>/dev/null)
          if echo "$BOOT_LOG" | grep -qi "BerkeOS"; then
            echo "PASS: BerkeOS detected in UEFI boot output"
            exit 0
          else
            echo "FAIL: BerkeOS not found in boot output"
            cat build/qemu_test.log
            cat build/serial.log || echo "(no serial log)"
            exit 1
          fi

  release:
    name: Create Release
    needs: [build, test-bios, test-uefi]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download ISO
        uses: actions/download-artifact@v4
        with:
          name: istikbalos-iso
          path: build

      - name: Check for existing release
        id: check_release
        run: |
          # Check if release already exists
          if gh release view v0.1 &>/dev/null; then
            echo "release_exists=true" >> $GITHUB_OUTPUT
          else
            echo "release_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        if: steps.check_release.outputs.release_exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v0.1
          name: IstikbalOS v0.1
          body: |
            ## IstikbalOS v0.1 - First Distribution Release

            Based on BerkeOS v0.6.3.

            ### Included
            - berkesh shell with tasks and turkiye commands
            - BerkeBex runtime (berun)
            - BIOS + UEFI boot tested in CI

            ### Artifact
            - istikbalos.iso
          draft: false
          prerelease: false
          files: build/istikbalos.iso
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload to existing release
        if: steps.check_release.outputs.release_exists == 'true'
        run: |
          gh release upload v0.1 build/istikbalos.iso --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
